/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://www.charterxe.com',
    generateRobotsTxt: true,
    // Optional: Add any additional configuration options here
  }